<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Post in Details</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <h2>Here is your post in details</h2>
    <% if (post) { %>
        <div class="post1">
            <p>post id: <%= post.id %></p>
        </div>
        <div class="post">
            <h3 class="user">@<%= post.username %></h3>
            <h4><%= post.content %></h4>
            <% if (post.photo) { %>
                <img src="data:image/jpeg;base64,<%= post.photo.buffer.toString('base64') %>" alt="Post Photo" style="max-width: 100%;">
            <% } %>
        </div>
       
    <% } else { %>
        <p>Post not found</p>
    <% } %>
</body>
</html>